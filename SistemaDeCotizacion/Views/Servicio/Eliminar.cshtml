@model SistemaDeCotizacion.Models.Servicio

@{
    ViewData["Title"] = "Eliminar Servicio";
    Layout = "_Layout";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow border-0">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0">Confirmar Eliminación</h5>
                </div>
                <div class="card-body bg-light">
                    <p class="fs-5">
                        ¿Estás seguro que deseas eliminar el servicio con el nombre
                        <strong class="text-dark">@Model.nombre_servicio</strong>?
                    </p>

                    <dl class="row mb-4">
                        <dt class="col-sm-4">Precio (S/.):</dt>
                        <dd class="col-sm-8">@Model.precio</dd>
                    </dl>

                    <form asp-controller="Servicio" asp-action="ConfirmacionEliminar" method="post">
                        <input type="hidden" name="id" value="@Model.servicio_id" />

                        <div class="d-flex justify-content-end gap-2">
                            <a asp-controller="Servicio" asp-action="Mostrar" class="btn btn-outline-dark">Cancelar</a>
                            <button type="submit" class="btn btn-danger">Eliminar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>