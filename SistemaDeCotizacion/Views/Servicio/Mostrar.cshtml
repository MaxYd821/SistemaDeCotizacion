@model List<SistemaDeCotizacion.Models.Servicio>

@{
    ViewData["Title"] = "Lista de Servicios";
    Layout = "_Layout";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h3 class="fw-bold text-dark">Gestión de Servicios</h3>
                <a asp-controller="Cliente" asp-action="Nuevo" class="btn btn-dark">Registrar Nuevo Servicio</a>
            </div>

            @if (!Model.Any())
            {
                <div class="alert alert-secondary text-center">
                    No hay servicios registrados actualmente.
                </div>
            }
            else
            {
                <div class="table-responsive shadow-sm border rounded">
                    <table class="table table-hover mb-0">
                        <thead class="table-dark text-white">
                            <tr>
                                <th>Nombre del Servicio</th>
                                <th>Precio (S/.)</th>
                                <th>Detalle del servicio</th>
                                <th class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody class="table-light">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.nombre_servicio</td>
                                    <td>@item.precio</td>
                                    <td>@item.detalle_servicio</td>
                                    <td class="text-center">
                                        <a asp-controller="Cliente" asp-action="Editar" asp-route-id="@item.servicio_id" class="btn btn-sm btn-outline-dark me-2">Editar</a>
                                        <a asp-controller="Cliente" asp-action="Eliminar" asp-route-id="@item.servicio_id" class="btn btn-sm btn-outline-danger">Eliminar</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>