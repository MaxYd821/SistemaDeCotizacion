@model SistemaDeCotizacion.Models.Rol

@{
    ViewData["Title"] = "Nuevo Rol";
    Layout = "_Layout";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-dark text-white">
                    <h4 class="mb-0">Registrar Rol</h4>
                </div>
                <div class="card-body bg-light">

                    @if (ViewData["mensaje"] != null)
                    {
                        <div class="alert alert-danger text-center">
                            @ViewData["mensaje"]
                        </div>
                    }

                    <form asp-action="Nuevo" method="post">
                        <div class="row g-3">

                            <div class="col-md-6">
                                <label asp-for="rol_nombre" class="form-label fw-semibold">Nombre del Rol</label>
                                <input asp-for="rol_nombre" class="form-control" placeholder="Nombre del Rol" required />
                                <span asp-validation-for="rol_nombre" class="text-danger"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="rol_descripcion" class="form-label fw-semibold">Descripción del rol</label>
                                <input asp-for="rol_descripcion" class="form-control" laceholder="Descripción del Rol" required />
                                <span asp-validation-for="rol_descripcion" class="text-danger"></span>
                            </div>

                            <div class="d-flex justify-content-end gap-2 mt-4">
                                <a asp-action="Mostrar" class="btn btn-outline-dark">Cancelar</a>
                                <button type="submit" class="btn btn-dark px-4">Guardar</button>
                            </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}